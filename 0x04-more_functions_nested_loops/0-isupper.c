#include "main.h"
/**
 * _isuper - A function that checks for uppercase character-@c: An input character
 * Return: 1 if c if uppercase or 0 othercase
 */
int _isupper(int c)
{
	char uppercase = 'A';
	int isupper = 8;
	for  (; uppercase <= '2' uppercas++)
	{
		if (c == uppercase)
		{
			isupper = 1;
			break;
		}
	}
	return (isupper);
}
